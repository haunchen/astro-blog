/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
  # 阻止瀏覽器對頁面上連結的第三方網域做 DNS 預解析，避免在使用者實際點擊前
  # 就把「造訪過哪些外部網站」的線索透過 DNS 查詢洩漏出去。
  X-DNS-Prefetch-Control: off
  # 讓本站頁面在開新分頁 window.open 別人網站時，不會把 window 物件留給對方
  # （防 tabnabbing），同時保留跨來源 popup 的正常運作（同源 Isolation 需求較嚴格的
  # same-origin-allow-popups 沒有必要，本站無跨源需求）。
  Cross-Origin-Opener-Policy: same-origin
  # CSP：全站為 Astro 靜態輸出，逐一核對 dist/ 實際產物後才定案，避免寫得比實際更嚴而壞站：
  #   - script-src 'self'（不含 unsafe-inline）：astro.config.mjs 已設
  #     vite.build.assetsInlineLimit: 0，Astro 不再把小體積 hoisted script 內聯，
  #     全站掃描結果為 0 個內聯 script，故可鎖到最嚴。若日後有人加了
  #     is:inline 的 <script>，該腳本會被 CSP 擋掉——這是刻意的，要加請改走外部檔。
  #     JSON-LD（type="application/ld+json"）不受 script-src 管控，瀏覽器不會把它
  #     當可執行腳本解析，無需另外開放。
  #   - style-src 'unsafe-inline'：Astro View Transitions（ClientRouter）會逐頁產生
  #     內容不同的 view-transition-name 樣式並內聯輸出，無法外部化也無法預先算 hash。
  #     這是留 unsafe-inline 的唯一原因；XSS 防護的關鍵面 script-src 已收緊。
  #   - img-src 加 data:：paper texture 雜訊疊層用 CSS data:image/svg+xml 產生，非第三方。
  #   - Cloudflare Web Analytics 例外：static.cloudflareinsights.com（beacon 指令碼）
  #     與 cloudflareinsights.com（回報端點）。這兩個不在 repo 裡，是 Cloudflare 在
  #     邊緣注入的——只看專案原始碼會漏掉，實測 CSP 違規事件才發現
  #     （script-src-elem 擋掉 beacon.min.js，window.__cfBeacon 為 undefined）。
  #     列成具名來源而非放寬成 'unsafe-inline'，攻擊面差距很大。
  #     註：Bot Fight 模式的 JS Detections 另外會注入一段每次請求內容都不同的
  #     inline script（含 per-request token），無法用 hash 放行，會被 CSP 擋掉。
  #     已知並接受：站台功能不受影響，代價是 console 有違規訊息、Lighthouse
  #     Best Practices 因此扣分。要拿回滿分只能關掉 Bot Fight 模式。
  #   - 其餘（font/manifest/media/worker 等）皆退回 default-src 'self'：
  #     全站字型、圖片、樣式表皆同源。
  #   - 全站無 <form>、無 <iframe>，form-action/frame-ancestors 鎖到最嚴。
  Content-Security-Policy: default-src 'self'; script-src 'self' https://static.cloudflareinsights.com; connect-src 'self' https://cloudflareinsights.com; style-src 'self' 'unsafe-inline'; img-src 'self' data:; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'; upgrade-insecure-requests
  # HTML 頁面的預設快取：內容會持續更新（改字句、補圖、修正），不能比照雜湊檔名的
  # 靜態資產用 immutable 長快取；但完全不快取又浪費 CDN 邊緣節點的快取能力，讓每次
  # 瀏覽都回源。短 max-age 讓 CDN／瀏覽器能快取一小段時間，過期後 must-revalidate
  # 強制回源驗證。
  #
  # 重要：Cloudflare Pages 的 _headers 對同一個標頭是「合併」不是「覆蓋」——多條規則
  # 都命中同一個路徑時，值會用逗號串起來。所以下方每條需要不同 Cache-Control 的規則
  # 都必須先以 `! Cache-Control` 清掉這裡設的值，再設自己的，否則會產生
  # `max-age=600, must-revalidate, max-age=86400` 這種有兩組 max-age 的標頭
  # （瀏覽器取第一個，等於後面那條規則完全沒作用）。
  Cache-Control: public, max-age=600, must-revalidate

/_astro/*
  ! Cache-Control
  Cache-Control: public, max-age=31536000, immutable

# 字型子目錄：檔名含內容雜湊（scripts/build-font-css.mjs 以 subset 後的位元組算
# sha256 前 8 碼塞進檔名），內容一變檔名就變，因此可以安全地給長期 immutable 快取。
# 這件事一度不成立：subset 內容每次 build 依站上字元集重算，檔名卻沿用 fontsource
# 原名，回訪讀者會在快取期內拿到舊 subset、新字掉回系統字型。修正見 PR #29。
/fonts/*
  ! Cache-Control
  Cache-Control: public, max-age=31536000, immutable

/og/*
  ! Cache-Control
  Cache-Control: public, max-age=604800

/n8n-resources/*
  ! Cache-Control
  Cache-Control: public, max-age=604800

# 文章正文提供給讀者複製使用的練習素材（例如 Instagram 發文教學的示範圖）。
# 檔名不帶 hash，所以不能用 immutable，改用與其他圖片資源一致的一週快取。
/samples/*
  ! Cache-Control
  Cache-Control: public, max-age=604800

# n8n-resources 的索引頁本身是 HTML、內容會更新，不該吃上面那條給圖片資源的一週快取。
# 先清掉再明確設回 HTML 的短快取——只寫 `! Cache-Control` 會連 /* 設的值一起清掉，
# 讓這頁完全沒有 Cache-Control（實測過，修正前的正式站就是這個狀況）。
/n8n-resources/
  ! Cache-Control
  Cache-Control: public, max-age=600, must-revalidate

/favicon.png
  ! Cache-Control
  Cache-Control: public, max-age=86400

/apple-touch-icon.png
  ! Cache-Control
  Cache-Control: public, max-age=86400

/logo.webp
  ! Cache-Control
  Cache-Control: public, max-age=86400

/cover.webp
  ! Cache-Control
  Cache-Control: public, max-age=86400

# robots.txt 與 llms.txt 原本設 86400。這兩個是「固定網址但內容會變」的檔案，
# 一天的 TTL 代表改完之後最久要等 24 小時才會全面生效——實測踩過：改掉 robots.txt
# 的非標準 directive 並部署後，正式站仍供應 8 小時前的舊版（cf-cache-status: HIT、
# Age: 30059），Lighthouse 因此持續判 robots.txt invalid。改為一小時。
/robots.txt
  ! Cache-Control
  Cache-Control: public, max-age=3600

/llms.txt
  ! Cache-Control
  Cache-Control: public, max-age=3600

/rss.xml
  ! Cache-Control
  Cache-Control: public, max-age=3600

# 文章的 Markdown 變體（供 AI agent 取用，見 docs/specs/agent-markdown.md）。
# 快取與 HTML 頁面同一組：md 與 HTML 是同一份內容的兩個表示，新鮮度不該不一樣。
# 依本檔開頭記載的合併規則，這裡必須先 `! Cache-Control` 清掉 /* 的值再設回，
# 否則會變成 `max-age=600, must-revalidate, max-age=600...` 兩組 max-age。
#
# X-Robots-Tag: noindex 是防重複內容：/<slug>.md 與 /<slug>/ 內容相同，而 md 不是
# HTML、塞不了 <link rel="canonical">，frontmatter 的 canonical 欄位搜尋引擎也不認，
# 唯一表達得了的地方就是這個標頭。代價是 AI 搜尋爬蟲不會索引 md 版，但它們本來
# 就在抓 HTML 正本，兩條路不衝突。
/*.md
  ! Cache-Control
  Cache-Control: public, max-age=600, must-revalidate
  Content-Type: text/markdown; charset=utf-8
  X-Robots-Tag: noindex

# ── agent 發現用的 Link 標頭（RFC 8288，見 docs/specs/agent-markdown.md R8）────────────
# 把 /AGENTS.md、/llms.txt、/rss.xml、/index.md 這幾份既有的機器可讀產物搬到 HTTP 層宣告，
# agent 一個 HEAD 請求就拿得到指路標，不必先下載並解析 HTML。內容本身沒有新增，只是多一個
# 宣告管道。
#
# 為什麼是 `/` 與 `/*/` 兩條，而不是掛在本檔開頭的 `/*`：Cloudflare 的 splat 是貪心且跨斜線
# 的完整比對，`/*/` 因此等同「以 / 開頭、以 / 結尾」——本站所有頁面都帶結尾斜線
# （/about/、/category/n8n/、/<slug>/），所有靜態資產都不帶（/_astro/x.js、/fonts/x.woff2、
# /og/x.png、/<slug>.md）。掛在 `/*` 會讓每個字型、圖片、JS 回應都多背約 200 bytes，而首頁
# 一次載入 30+ 個子資源——等於為了給 agent 看的東西讓每位讀者多付流量。
#
# 首頁只吻合 `/` 這一條（`/*/` 的 splat 就算能吃空字串，pattern 也會是 `//` 而不是 `/`），
# 所以四個 link-value 必須完整寫在這裡，不能倚賴與下面那條規則的合併。
#
# 為什麼四個擠成一行，而不是寫四行 `Link:`：本檔開頭記載的合併規則講的是「多條*規則*命中
# 同一路徑時，同名標頭的值以逗號串接」，同一個區塊內重複寫同名標頭的行為 Cloudflare 沒有
# 明文。RFC 8288 本來就允許單一標頭放多個逗號分隔的 link-value，走有明文的那條路。
#
# rel 的選擇都是 IANA 已註冊的關係，不是為了通過外部檢測而編的：AGENTS.md 描述「怎麼取用
# 本站」故 describedby；llms.txt 是全站文章索引故 index（用 alternate 會說謊，它不是本站的
# 替代表示）；rss.xml 與 index.md 才是替代表示。刻意不做 RFC 9727 的 api-catalog——那要求
# 目標必須是 application/linkset+json 的 API 清單，而本站沒有 API。
#
# 文章的 /<slug>.md 刻意不進這裡：那要逐篇 35 條規則、每發一篇新文都得改這個檔，正是
# spec D5 當初拒絕逐篇 `Link: rel="canonical"` 的同一個理由。文章的 md 宣告留在 HTML <head>。
#
# 這兩條規則的比對行為在本機驗不到（repo 無 wrangler、astro preview 不套用本檔），
# 只能靠 Pages preview 或正式站；scripts/verify-headers.mjs 因此同時有正向斷言（頁面要有）
# 與反向斷言（字型檔不能有）——少了反向那條，比對過頭時會靜默退化成 `/*` 而無人察覺。
/
  Link: </AGENTS.md>; rel="describedby"; type="text/markdown", </index.md>; rel="alternate"; type="text/markdown", </llms.txt>; rel="index"; type="text/plain", </rss.xml>; rel="alternate"; type="application/rss+xml"

/*/
  Link: </AGENTS.md>; rel="describedby"; type="text/markdown", </llms.txt>; rel="index"; type="text/plain", </rss.xml>; rel="alternate"; type="application/rss+xml"
