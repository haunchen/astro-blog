/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=()
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
  # 阻止瀏覽器對頁面上連結的第三方網域做 DNS 預解析，避免在使用者實際點擊前
  # 就把「造訪過哪些外部網站」的線索透過 DNS 查詢洩漏出去。
  X-DNS-Prefetch-Control: off
  # 讓本站頁面在開新分頁 window.open 別人網站時，不會把 window 物件留給對方
  # （防 tabnabbing），同時保留跨來源 popup 的正常運作（同源 Isolation 需求較嚴格的
  # same-origin-allow-popups 沒有必要，本站無跨源需求）。
  Cross-Origin-Opener-Policy: same-origin
  # CSP：全站為 Astro 靜態輸出，逐一核對 dist/ 實際產物後才定案，避免寫得比實際更嚴而壞站：
  #   - script-src 'self'（不含 unsafe-inline）：astro.config.mjs 已設
  #     vite.build.assetsInlineLimit: 0，Astro 不再把小體積 hoisted script 內聯，
  #     全站 104 頁掃描結果為 0 個內聯 script，故可鎖到最嚴。若日後有人加了
  #     is:inline 的 <script>，該腳本會被 CSP 擋掉——這是刻意的，要加請改走外部檔。
  #     JSON-LD（type="application/ld+json"）不受 script-src 管控，瀏覽器不會把它
  #     當可執行腳本解析，無需另外開放。
  #   - style-src 'unsafe-inline'：Astro View Transitions（ClientRouter）會逐頁產生
  #     內容不同的 view-transition-name 樣式並內聯輸出，無法外部化也無法預先算 hash。
  #     這是留 unsafe-inline 的唯一原因；XSS 防護的關鍵面 script-src 已收緊。
  #   - img-src 加 data:：paper texture 雜訊疊層用 CSS data:image/svg+xml 產生，非第三方。
  #   - Cloudflare Web Analytics 例外：static.cloudflareinsights.com（beacon 指令碼）
  #     與 cloudflareinsights.com（回報端點）。這兩個不在 repo 裡，是 Cloudflare 在
  #     邊緣注入的——只看專案原始碼會漏掉，實測 CSP 違規事件才發現
  #     （script-src-elem 擋掉 beacon.min.js，window.__cfBeacon 為 undefined）。
  #     列成具名來源而非放寬成 'unsafe-inline'，攻擊面差距很大。
  #     註：Bot Management 的 JS Detections 不需要例外——Cloudflare 以同源路徑
  #     （/ql0n/ 之類的隨機路徑）提供，'self' 本來就允許。
  #   - 其餘（font/manifest/media/worker 等）皆退回 default-src 'self'：
  #     全站字型、圖片、樣式表皆同源。
  #   - 全站無 <form>、無 <iframe>，form-action/frame-ancestors 鎖到最嚴。
  Content-Security-Policy: default-src 'self'; script-src 'self' https://static.cloudflareinsights.com; connect-src 'self' https://cloudflareinsights.com; style-src 'self' 'unsafe-inline'; img-src 'self' data:; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; object-src 'none'; upgrade-insecure-requests
  # HTML 頁面的預設快取（未被下方更精確規則覆蓋的路徑，含 /、/about/、/articles/、
  # /category/*、/tag/*、各篇文章頁）：內容會持續更新（改字句、補圖、修正），不能比照
  # /_astro/* 用 immutable 長快取；但完全不快取又浪費 CDN 邊緣節點的快取能力，讓每次
  # 瀏覽都回源。短 max-age 讓 CDN／瀏覽器能快取一小段時間，過期後 must-revalidate
  # 強制回源驗證，兼顧「改完內容多久會生效」與「CDN 快取命中率」。下方各條更精確的
  # 路徑規則（/_astro/*、/og/* 等）會覆蓋這裡的 Cache-Control。
  Cache-Control: public, max-age=600, must-revalidate

/_astro/*
  Cache-Control: public, max-age=31536000, immutable

# 字型子目錄：檔名固定（無 hash），比照 /_astro/* 給長期 immutable 快取。
# 前提是「換字型檔案必須同時改檔名」——若直接覆蓋同名檔案，舊的 immutable 快取
# 不會失效，使用者會繼續吃到舊字型，換檔時務必連檔名一起改。
/fonts/*
  Cache-Control: public, max-age=31536000, immutable

/og/*
  Cache-Control: public, max-age=604800

/n8n-resources/*
  Cache-Control: public, max-age=604800

/n8n-resources/
  ! Cache-Control

/favicon.png
  Cache-Control: public, max-age=86400

/apple-touch-icon.png
  Cache-Control: public, max-age=86400

/logo.webp
  Cache-Control: public, max-age=86400

/cover.webp
  Cache-Control: public, max-age=86400

/robots.txt
  Cache-Control: public, max-age=86400

/llms.txt
  Cache-Control: public, max-age=86400

/rss.xml
  Cache-Control: public, max-age=3600
